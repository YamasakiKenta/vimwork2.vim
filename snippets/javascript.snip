snippet leaflet
    (function(map){
        let _back = false;
        window.onpopstate = function() {
          if(_back){ 
            SetViewFromUrl(); 
          }
          _back = true;
        }
        function SetViewFromUrl() {
            let pos = location.hash.slice(2).split(',');
            map.setView(pos, pos[2]);
        }
        function round(num, keta){
            let i = Math.pow(10, keta);
            return Math.round(num*i) / i;
        }
        map.on('moveend', function(){
            let pos = map.getCenter();
            let strs = [
                round(pos.lat,6),
                round(pos.lng,6),
                map.getZoom(),
            ]
            location.hash = '#/'+strs.join(',')
            _back = false;
        });
        if(location.hash==''){
          location.hash = '#/34.928305,134.008312,12'
        }
        SetViewFromUrl();
    })(map);
snippet vue-rooter
alias vuer
    // <router-view></router-view>
    const NotFound = { template: '<p>Page not found</p>' }
    const Home = { template: '<p>Home</p>' }
    const Login = { template: '#login' }
    let router = new VueRouter({
        routes: [
            { path: '*', component: NotFound },
            { path: '/', component: Home },
            { path: '/', component: Login },
        ],
    })
    router.beforeEach( (to, from, next) => {
        console.log(to)
        next()
    })
    let app = new Vue({
        el : '#app',
        router,
    })
snippet vue
    var app = new Vue({
      el : '#app',
      data() {
        return {
        }
      },
      // methods watch computed created mounted
    });
snippet localstrage
alias save
    localStorage.setItem('root', JSON.stringify(this.root))
    JSON.parse(localStorage.getItem('root'))
    save: function(key, val) {
      localStorage.setItem(key, JSON.stringify(val))
    },
    load: function(key) {
      return JSON.parse(localStorage.getItem(key))
    }
